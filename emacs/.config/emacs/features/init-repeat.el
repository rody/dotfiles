;;; init-repeat.el -- configuration for repeat mode -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package repeat
  :ensure nil
  :config
  (repeat-mode 1))

(provide 'init-repeat)
;;; init-repeat.el ends here
