;;; init-package-extensions.el -- package extension -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package gcmh
  :ensure t
  :disabled t
  :init
  (gcmh-mode +1))

(provide 'init-package-extensions)
;;; init-package-extensions.el ends here
