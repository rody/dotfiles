;;; init-writeroom.el --  -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package writeroom-mode
  :ensure t
  :bind (("C-c t D" . writeroom-mode)))

(provide 'init-writeroom)
;;; init-writeroom.el ends here
